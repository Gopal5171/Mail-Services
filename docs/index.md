# `RPA Mail Service` User Guide

??? info "`RPA Mail Service` Project"

    As soon as users send an email to bot mailbox, this service can extract data from email and handle user's request

!!! info

    RPA team can use this project as a blue print python project. Please change information in:
    mkdocs.yml, pyproject.toml [^1]

[^1]: Find guide on Docupedia.

## Installation

First, [install Poetry](https://python-poetry.org/docs/#installation):

=== "Linux/macOS"

    ```bash
    curl -sSL https://install.python-poetry.org | python3 -
    ```

=== "Windows"

    ```powershell
    (Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | py -
    ```

Then install the `fact` package and its dependencies:

```bash
poetry install
```

Activate the virtual environment created automatically by Poetry:

```bash
poetry shell
```

## Quick Start



```python

```

